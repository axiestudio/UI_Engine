{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "gsap-kinetic-marquee",
  "type": "registry:block",
  "title": "GSAP: Kinetic Marquee",
  "description": "Velocity-reactive marquee band — speed, skew and direction follow your scroll (GSAP) Companion styles: @import \"@/components/gsap-kinetic-marquee/gsap-kinetic-marquee.css\".",
  "author": "UI Engine <https://axiestudio.github.io/UI_Engine/>",
  "categories": [
    "ui-engine",
    "content"
  ],
  "dependencies": [
    "clsx@^2.1.1",
    "gsap@^3.15.0",
    "lucide-react@^1.34.0",
    "motion@^13.1.1",
    "tailwind-merge@^3.6.0"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "gsap-kinetic-marquee/components/gsap-kinetic-marquee/GsapKineticMarquee.tsx",
      "type": "registry:component",
      "target": "@components/gsap-kinetic-marquee/GsapKineticMarquee.tsx",
      "content": "/**\n * gsap-kinetic-marquee — vendored from the UI Engine preset library (axiestudio).\n * Install: npx shadcn@latest add https://axiestudio.github.io/UI_Engine/registry/r/gsap-kinetic-marquee.json\n * Source of truth: UI/ — do not edit vendored copies blindly.\n */\nimport * as React from \"react\"\nimport gsap from \"gsap\"\nimport { ScrollTrigger } from \"gsap/ScrollTrigger\"\nimport { cn } from \"@/lib/utils\"\nimport { InView } from \"@/components/primitives/in-view\"\n\ngsap.registerPlugin(ScrollTrigger)\n\n// ═══ JOB         Say the one sentence the room must remember.\n// ═══ EMOTION     A shop sign in a storm — alive, leaning into the wind.\n// ═══ SIGNATURE   Two ribbon rows run opposite directions and answer your\n//                 scroll: velocity drives their speed, skews the type and,\n//                 past a threshold, flips the whole parade's direction.\n\nexport type GsapKineticMarqueeProps = {\n  /** Row A — fills the ribbon on repeat. */\n  lineA?: string\n  /** Row B — runs against row A. */\n  lineB?: string\n  label?: string\n  tone?: \"ink\" | \"paper\"\n  className?: string\n}\n\nexport function GsapKineticMarquee({\n  lineA = \"BOOK THE BOARD · TRUST THE CHAIR ·\",\n  lineB = \"quiet hands · loud work · sharp scissors ·\",\n  label = \"Kinetic ribbon — answers your scroll\",\n  tone = \"ink\",\n  className,\n}: GsapKineticMarqueeProps) {\n  const rootRef = React.useRef<HTMLDivElement>(null)\n  const rowA = React.useRef<HTMLDivElement>(null)\n  const rowB = React.useRef<HTMLDivElement>(null)\n  const reduce = React.useMemo(() => typeof window !== \"undefined\" && window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches, [])\n\n  React.useLayoutEffect(() => {\n    if (reduce) return\n    const ctx = gsap.context(() => {\n      const proxy = { skew: 0, dir: 1 }\n      const skewSetter = gsap.quickSetter(rowA.current, \"skewY\", \"deg\")\n      const clampSkew = gsap.utils.clamp(-8, 8)\n\n      const xToA = gsap.to(rowA.current, { xPercent: -25, ease: \"none\", duration: 24, repeat: -1 })\n      const xToB = gsap.to(rowB.current, { xPercent: 25, ease: \"none\", duration: 30, repeat: -1 })\n\n      ScrollTrigger.create({\n        onUpdate: (self) => {\n          const v = self.getVelocity() / 260\n          const skew = clampSkew(v)\n          // lean into direction of travel\n          proxy.skew = gsap.utils.interpolate(proxy.skew, skew, 0.18)\n          skewSetter(-proxy.skew)\n          // fast scroll flips the parade\n          if (Math.abs(v) > 2.4) proxy.dir = v > 0 ? 1 : -1\n          xToA.timeScale(gsap.utils.interpolate(xToA.timeScale(), (0.6 + Math.min(Math.abs(v), 2.4)) * proxy.dir, 0.12))\n          xToB.timeScale(gsap.utils.interpolate(xToB.timeScale(), (0.6 + Math.min(Math.abs(v), 2.4)) * -proxy.dir, 0.12))\n        },\n      })\n    }, rootRef)\n    return () => ctx.revert()\n  }, [reduce])\n\n  const Row = ({ text, outline, refEl, ariaHidden }: { text: string; outline?: boolean; refEl: React.RefObject<HTMLDivElement | null>; ariaHidden?: boolean }) => (\n    <div className=\"flex overflow-hidden py-1\" aria-hidden={ariaHidden}>\n      <div ref={refEl} className=\"flex w-max shrink-0 items-center whitespace-nowrap will-change-transform\">\n        {[0, 1, 2, 3].map((rep) => (\n          <span\n            key={rep}\n            className={cn(\n              \"px-4 font-display text-[44px] font-black uppercase leading-none tracking-tight sm:text-[58px]\",\n              outline\n                ? \"text-transparent [-webkit-text-stroke:1.5px_hsl(var(--background)/0.55)]\"\n                : \"text-background\",\n            )}\n          >\n            {text}\n          </span>\n        ))}\n      </div>\n    </div>\n  )\n\n  return (\n    <section ref={rootRef} className={cn(\"relative isolate overflow-hidden w-full bg-foreground py-12 sm:py-16\", className)} aria-label={label}>\n      <InView once variants={{ hidden: { opacity: 0 }, visible: { opacity: 1 } }} transition={{ duration: 0.6 }}>\n        <Row text={lineA} refEl={rowA} />\n        <Row text={lineB} outline refEl={rowB} ariaHidden />\n        <p className=\"mt-4 text-center font-mono text-[9px] font-bold uppercase tracking-[0.3em] text-background/40\">\n          {label}\n        </p>\n      </InView>\n    </section>\n  )\n}\n"
    },
    {
      "path": "gsap-kinetic-marquee/components/primitives/in-view.tsx",
      "type": "registry:component",
      "target": "@components/primitives/in-view.tsx",
      "content": "/**\n * gsap-kinetic-marquee — vendored from the UI Engine preset library (axiestudio).\n * Install: npx shadcn@latest add https://axiestudio.github.io/UI_Engine/registry/r/gsap-kinetic-marquee.json\n * Source of truth: UI/ — do not edit vendored copies blindly.\n */\n/**\n * Vendored verbatim from motion-primitives (https://motion-primitives.com) by ibelick.\n * Upstream: https://github.com/ibelick/motion-primitives/blob/main/components/core/in-view.tsx\n * Fetched 2026-08-29 from main branch. Do not edit unless intentionally adopting upstream changes.\n */\n'use client';\n// Local patch for workspace tsconfig (verbatimModuleSyntax): type-only imports only, no behavior change.\nimport { useRef, useState, type ReactNode } from 'react';\nimport { motion, useInView, type Variant, type Transition, type UseInViewOptions,  } from 'motion/react';\n\nexport type InViewProps = {\n  children: ReactNode;\n  variants?: {\n    hidden: Variant;\n    visible: Variant;\n  };\n  transition?: Transition;\n  viewOptions?: UseInViewOptions;\n  as?: React.ElementType;\n  once?: boolean;\n  delay?: number;\n  className?: string;\n};\n\nconst defaultVariants = {\n  hidden: { opacity: 0 },\n  visible: { opacity: 1 },\n};\n\nexport function InView({\n  children,\n  variants = defaultVariants,\n  transition,\n  delay,\n  viewOptions,\n  as = 'div',\n  once\n}: InViewProps) {\n  const ref = useRef(null);\n  const isInView = useInView(ref, viewOptions);\n\n  const [isViewed, setIsViewed] = useState(false)\n\n  const MotionComponent = motion[as as keyof typeof motion] as typeof as;\n\n  return (\n    <MotionComponent\n      ref={ref}\n      initial='hidden'\n      onAnimationComplete={() => {\n        if (once) setIsViewed(true)\n      }}\n      animate={(isInView || isViewed) ? \"visible\" : \"hidden\"}\n\n      variants={variants}\n      transition={delay !== undefined ? { ...transition, delay } : transition}\n    >\n      {children}\n    </MotionComponent>\n  );\n}\n"
    },
    {
      "path": "gsap-kinetic-marquee/gsap-kinetic-marquee.css",
      "type": "registry:file",
      "target": "@components/gsap-kinetic-marquee/gsap-kinetic-marquee.css",
      "content": "/* gsap-kinetic-marquee companion styles — @import this file in your CSS (tokens come from your theme). */\n/* ─── BOLD PROFESSIONAL Brandkit ──────────────────────────────────────────\n   Palette: PURE WHITE #fff + JET BLACK #121212 — very strong contrast\n   Fonts: Inter Black/ExtraBold (UI) + Space Grotesk Bold (display) + JetBrains Mono Bold\n   Sizes: 15px UI base, tight tracking, bold weights\n─────────────────────────────────────────────────────────────────────────── */\n\n\n\n\n::selection {\n  background: hsl(var(--foreground));\n  color: hsl(var(--background));\n}\n.no-scrollbar::-webkit-scrollbar { display: none; }\n.no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }\n"
    }
  ]
}
