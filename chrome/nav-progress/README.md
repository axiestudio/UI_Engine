# nav-progress

**JOB** — Navbar with scroll progress — a top progress rail plus sticky bar
**SIGNATURE** — Navbar with scroll progress — a top progress rail plus sticky bar

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
