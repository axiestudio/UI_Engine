# hero-ink — Hero — full-bleed ink

Forced dark band: breathing glow orb anchored asymmetrically, corner ticks framing the viewport, coordinates strip along the bottom.

**Provenance:** shadcn/ui `new-york-v4`; Motion-Primitives; in-repo handcraft kit (grain, ticks, rails, mono labels).

```tsx
// see package README examples in UI/<name>/src — APIs are prop-driven and
// mirror the existing presets (links, cta, brand, lines, rotating, chips…)
```
