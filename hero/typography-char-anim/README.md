# typography-char-anim

**JOB** — Character animation headline — text-scramble reveal on scroll into view
**SIGNATURE** — Character animation headline — text-scramble reveal on scroll into view

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
