# hero-sticky-fullscreen

**JOB** — Sticky fullscreen hero — the hero pins as the next section overlaps it
**SIGNATURE** — Sticky fullscreen hero — the hero pins as the next section overlaps it

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
