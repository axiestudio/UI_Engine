# hero-video

**JOB** — Hero with video background — muted looping video behind the statement
**SIGNATURE** — Hero with video background — muted looping video behind the statement

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
