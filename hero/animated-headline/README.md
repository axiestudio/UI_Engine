# animated-headline

**JOB** — Animated headline — per-word / per-char reveal via TextEffect
**SIGNATURE** — Animated headline — per-word / per-char reveal via TextEffect

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
