# hero-floating-cards

**JOB** — Hero with floating cards — layered proof cards over the headline
**SIGNATURE** — Hero with floating cards — layered proof cards over the headline

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
