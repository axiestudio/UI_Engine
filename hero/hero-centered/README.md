# hero-centered

**JOB** — Centered hero — a single column statement with pill eyebrow and CTA row
**SIGNATURE** — Centered hero — a single column statement with pill eyebrow and CTA row

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
