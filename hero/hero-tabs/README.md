# hero-tabs

**JOB** — Tabbed hero — TransitionPanel cycles statements on the same canvas
**SIGNATURE** — Tabbed hero — TransitionPanel cycles statements on the same canvas

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
