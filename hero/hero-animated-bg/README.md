# hero-animated-bg

**JOB** — Hero with animated background — drifting gradient / glow canvas
**SIGNATURE** — Hero with animated background — drifting gradient / glow canvas

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
