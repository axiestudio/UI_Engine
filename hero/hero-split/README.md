# hero-split

**JOB** — Split hero — a content column beside a framed image, editorial scale
**SIGNATURE** — Split hero — a content column beside a framed image, editorial scale

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
