# typography-outline

**JOB** — Outline typography — giant stroked display type for editorial impact
**SIGNATURE** — Outline typography — giant stroked display type for editorial impact

## Sources

Vendored: shadcn/ui `button` (new-york-v4), motion-primitives (`in-view`), the in-repo
`handcraft` kit. Motion via `motion/react`. Re-themes with the engine tokens.
