# GsapSplitBand

Composite section mixing the house system (shadcn tokens · motion · handcraft kit) with
gsap — see the component header for JOB / EMOTION / SIGNATURE.

- Interaction library: `gsap` (peer dependency, tree-shaken)
- Tokens only · keyboard reachable · `prefers-reduced-motion` honored
