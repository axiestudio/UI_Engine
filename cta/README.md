# cta — UI preset

Call-to-action band: `tone="ink"|"paper"|"ghost"`, badge chip, optional shimmer headline (motion-primitives TextShimmer), `Magnetic` primary button with `BorderTrail` sweep, optional secondary + inline note. `primary.href`→anchor, `onClick`→button.

**Provenance:** Watermelon `cta-1`/`cta-2` (verbatim), `badge`/`button`; Motion-Primitives `magnetic`, `text-shimmer`, `border-trail`, `in-view`.
